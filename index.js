Translate a given word or phrase from English into Maori using the Google Translate API.

- how to include a space in the url?

1) Set up the elements

  $.ajaxSend(function(event, xhr, settings) {
    /* stuff to do before an AJAX request is sent */;
  });

  $.$.ajaxSetup({
    url: '/path/to/file',
    type: 'GET',
    dataType: 'json',
    data: {param1: 'value1'},
  })
  .done(function() {
    console.log("success");
  })
  .fail(function() {
    console.log("error");
  })
  .always(function() {
    console.log("complete");
  });
